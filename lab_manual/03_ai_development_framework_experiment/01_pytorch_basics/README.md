# 01 PyTorch Basics

## Scope

This directory organizes code from `1.2 PyTorch Basics` in the lab guide. It covers:

```text
Tensor basics
Dataset loading
ImageFolder loading
Custom Dataset example
```

## File

```text
01_pytorch_basics_pdf_original.py
```

The ImageFolder section expects a `./flower_photos_train/` directory. The custom Dataset example uses placeholder paths for `image_dir` and `label_file`.
